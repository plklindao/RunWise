// Serviço de comunicação com o backend
// - Configure a baseURL do axios (URL do servidor)
// - Inclua o token JWT no header de todas as requisições
// - Exporte funções: login(email, senha), register(nome, email, senha),
//   getMetrics(), getSummary()
