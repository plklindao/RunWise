// Modelo de usuário (armazenamento em JSON)
// findByEmail(email) → busca e retorna usuário pelo email
// save(user)         → adiciona novo usuário ao arquivo
// getAll()           → retorna lista de todos os usuários
